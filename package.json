{
  "name": "team-task-manager",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "cd frontend && npm install && cd ../backend && npm install && npx prisma generate",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npx prisma migrate deploy && node index.js"
  }
}
